//#include <iostream>
//using namespace std;
//
//int main() {
//	int x, y, z, a, b;
//	cin >> x >> y >> z >> a >> b;
//	int burger = (x < (y < z ? y : z) ? x : (y < z ? y : z));
//	int beverage = a < b ? a : b;
//
//	cout << burger + beverage - 50;
//}